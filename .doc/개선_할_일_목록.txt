✅ 개선 할 일 목록
출처: https://www.notion.so/344d2c6518f381db8efdf2c7dd72abe1
상위 페이지: KIS 자동매매 CLI

> 2026-04-17 코드 분석 기반으로 도출된 개선 항목입니다. 우선순위 순으로 정렬되어 있습니다.

---

## 🔴 Critical — 즉시 수정 필요

- [ ] [보안] `token_cache.json` 보안 이슈
    - access_token이 프로젝트 루트에 평문 JSON으로 저장됨
    - `.gitignore`에 `token_cache.json` 추가
    - 저장 경로를 `~/.kis/` 같은 홈 디렉토리로 이동
    - 관련 파일: `src/auth.py:46`

- [ ] [안정성] `trader.py` rate limit 미적용
    - `fetcher.py`에는 `_rate_limit()` 있지만 `trader.py`의 직접 `requests` 호출엔 없음
    - 잔고조회(`_inquire_balance_raw`), 주문(`_order_request`) API에 rate limit + retry 추가 필요
    - 관련 파일: `src/trader.py:31`, `src/trader.py:80`

- [ ] [버그] `daily_loss` 실현손익 계산 오류
    - `h["profit_loss"]`는 평가손익(미실현)이라 서킷브레이커 기준값이 틀림
    - 매도 체결 후 실현손익을 별도로 계산해 누적해야 함
    - 관련 파일: `src/scheduler.py:35`

---

## 🟠 High — 기능 안정성

- [ ] [안정성] `daily_loss` 재시작 시 초기화
    - `state`가 메모리에만 있어 프로세스 재시작 시 당일 손실 누적 초기화됨
    - 파일 또는 로컬 DB에 일별 상태 저장 필요
    - 관련 파일: `src/scheduler.py:103`

- [ ] [안정성] 주문 API retry/오류처리 없음
    - `_order_request`에 retry 로직 없음 → 일시적 네트워크 오류 시 주문 누락
    - `fetcher.py`의 `_api_get`처럼 retry 패턴 동일하게 적용
    - 관련 파일: `src/trader.py`

- [ ] [구조] 환경변수 중앙 관리 없음
    - `os.getenv()`가 `scheduler.py`, `analyzer.py`, `trader.py`에 분산
    - `src/config.py` 모듈로 통합하여 단일 진입점 확보

---

## 🟡 Medium — 개선 권장

- [ ] [정확도] RSI 계산 방식 개선
    - 단순 SMA 방식 사용 중, Wilder's SMMA 방식과 값 차이 발생 가능
    - 표준 RSI 공식으로 교체 권장
    - 관련 파일: `src/analyzer.py:10`

- [ ] [기능] `analyze` 명령어 보유 종목 연동
    - 현재 `avg_price=0`으로 고정 호출 → 손절/익절 체크 스킵됨
    - 보유 종목이면 `avg_price`를 실제 평균단가로 넘기도록 수정
    - 관련 파일: `main.py:66`

- [ ] [기능] `history` 명령어 날짜 지정 옵션 추가
    - 현재 오늘 날짜만 고정 조회
    - `--date 20260416` 옵션 추가
    - 관련 파일: `main.py:35`

- [ ] [로깅] 로그 파일 전략 개선
    - INFO 레벨(거래 신호, 매매 내역)이 콘솔에만 출력되고 파일 저장 안 됨
    - `trades_{today}.csv` 파일 쓰기 시 파일 잠금(lock) 처리 없음
    - 관련 파일: `src/logger.py`

---

## 🔵 Low — 장기 개선

- [ ] [테스트] 단위 테스트 코드 작성
    - 전 모듈에 테스트 코드 전무
    - API 모킹(`unittest.mock`) 기반 테스트 환경 구축

- [ ] [패키징] `pyproject.toml` 추가
    - `pip install -e .` 설치 불가 상태
    - 버전 정보, 진입점(`kis-trader` 커맨드) 정의 필요

- [ ] [모니터링] 프로세스 상태 확인 수단 없음
    - 실행 중인 자동매매 상태를 외부에서 확인 불가
    - PID 파일 또는 상태 파일(`status.json`) 생성 고려

- [ ] [전략] 포지션 사이징 개선
    - `amount // current_price` 고정 수량만 지원
    - 변동성(ATR) 기반 사이징 또는 비중 분산 로직 고려
    - 관련 파일: `src/trader.py:44`

---

## 📊 요약

| 우선순위     | 항목 수 | 핵심 내용                          |
|--------------|---------|------------------------------------|
| 🔴 Critical  | 3       | 보안, rate limit, 손익 계산 버그   |
| 🟠 High      | 3       | 상태 영속성, retry, 설정 중앙화    |
| 🟡 Medium    | 4       | 분석 정확도, CLI UX, 로깅          |
| 🔵 Low       | 4       | 테스트, 패키징, 모니터링, 전략     |
