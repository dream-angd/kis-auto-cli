# 다음 세션(세션 6) 인계 프롬프트

> 작성일: 2026-05-02 (세션 5 완료 시점)
> 다음 세션 범위: **세션 6 — 포지션 사이징 + 단위 테스트 (ATR 기반 사이징 / 단위 테스트)**

---

## 1. 이전 세션(세션 5) 결과 요약

### 완료 항목

- **⑩ 로그 전략 개선 (`src/logger.py`)**
  - `_csv_lock = threading.Lock()` 추가 → `log_trade()` 내 CSV 쓰기를 Lock으로 보호
  - `write_header` 체크를 Lock 내부로 이동 → 헤더 중복 쓰기 방지
  - INFO 레벨 파일 핸들러 추가: `logs/app.log` (TimedRotatingFileHandler, midnight, backupCount=30)
  - ERROR 핸들러는 `logs/error.log`로 유지 (기존과 동일)

- **⑬ 프로세스 status.json (`src/config.py`, `src/scheduler.py`)**
  - `src/config.py`: `get_status_path()` 추가 → `~/.kis/status.json`
  - `src/scheduler.py`: `_write_status()` / `_clear_status()` 함수 추가
    - `run_loop()` 진입 직후 `_write_status()` 호출 → PID, started_at, mode 기록
    - `finally` 블록에서 `_clear_status()` 호출 → 정상/비정상 종료 모두 삭제
  - `import os` 추가

- **⑫ pyproject.toml (신설)**
  - `pyproject.toml` 프로젝트 루트에 생성
  - build-system: setuptools>=68
  - dependencies: requirements.txt 4개 항목 통합
  - entry point: `kis-trader = "main:main"`
  - requires-python: >=3.11

### 변경 파일
- `src/logger.py` — threading.Lock 추가, app.log 핸들러 신설
- `src/config.py` — `get_status_path()` 추가 (line 54-56)
- `src/scheduler.py` — `import os`, `_write_status()`, `_clear_status()`, `run_loop()` 수정
- `pyproject.toml` — 신설

### 자체 검증
- `python -m py_compile` 3파일 모두 구문 오류 없음 확인

---

## 2. 세션 6에서 주의해야 할 인계사항

1. **세션 1~5 API 사용 규칙 유지**
   - 환경변수: `from src import config; config.get_*()` 사용
   - 로그: `from src.logger import log_info, log_error, log_trade, log_signal`

2. **ATR 기반 포지션 사이징 (`src/trader.py`, `src/analyzer.py`)**
   - `analyzer.py`에서 ATR 계산 후 반환값에 추가 필요
   - `trader.py`의 `buy()` 또는 별도 함수에서 ATR 기반 수량 산정 필요
   - `scheduler.py`의 `_check_targets()` 에서 max_buy 대신 ATR 기반 수량 사용 검토

3. **단위 테스트 (`tests/` 신설)**
   - `tests/` 디렉토리 신설
   - 모든 모듈 변경이 끝났으므로 테스트 재작성 비용 없음
   - pytest 사용 권장 (`pyproject.toml`에 pytest 설정 추가 가능)

4. **pyproject.toml에 pytest 설정 추가 가능**
   - `[tool.pytest.ini_options]` 섹션 추가 검토

---

## 3. 세션 6 작업 범위 (재확인)

| 항목 | 우선순위 | 대상 파일 |
|------|----------|-----------|
| ⑭ ATR 기반 포지션 사이징 | 🔵 Low | `src/trader.py`, `src/analyzer.py` |
| ⑪ 단위 테스트 | 🔵 Low | `tests/` (신설), `pyproject.toml` 수정 |

---

## 4. 다음 세션 시작 프롬프트 (복사용)

```
/forge

C:\Users\lgw1156\Desktop\lgw\999_개인\kis-auto-cli\.doc\작업_플랜.md 와
C:\Users\lgw1156\Desktop\lgw\999_개인\kis-auto-cli\.doc\다음_세션_프롬프트.md 를 읽고
세션 [6]의 작업 범위만 진행한다.

[수행 단계]
1. 작업_플랜.md에서 이번 세션의 범위와 이전 세션의 인계사항을 확인한다.
2. 세션 범위에 대해 설계안을 제시하고 사용자 확인을 받는다.
3. 구현 → 자체 검증을 수행한다.
4. 결과 요약 + 다음 세션 인계 정보를 .doc/다음_세션_프롬프트.md 에 갱신한다.
   (마지막 세션이면 .doc/작업_완료_보고서.md 를 작성한다.)

제약사항:
- 이번 세션 범위 외 항목은 손대지 않는다.
- 각 단계에서 사용자 확인이 필요한 경우 진행을 멈추고 묻는다.
- 한국어로 응답한다.
```
