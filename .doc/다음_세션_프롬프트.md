# 다음 세션(세션 4) 인계 프롬프트

> 작성일: 2026-05-02 (세션 3 완료 시점)
> 다음 세션 범위: **세션 4 — 분석 정확도 + CLI UX (RSI Wilder SMMA / analyze 보유종목 연동 / history --date 옵션)**

---

## 1. 이전 세션(세션 3) 결과 요약

### 완료 항목
- **③ daily_loss 실현손익 계산 정정 (🔴Critical)**
  - 기존: `h["profit_loss"]`(평가손익·미실현) → 서킷브레이커 기준값 오류
  - 변경: `(result["current_price"] - h["avg_price"]) * h["quantity"]`로 실현손익 계산
  - `result["current_price"]`는 `analyze()` 반환값 (매도 신호 판단 시 현재가 = 시장가 근사치)

- **④ daily_loss 재시작 영속화 (🟠High)**
  - `~/.kis/state.json`에 `{"date": "...", "daily_loss": 0, "consecutive_losses": 0}` 구조로 저장
  - `_load_state()`: 날짜 일치 시 복원, 불일치·파일 없음·파싱 오류 시 당일 초기화
  - `_save_state()`: 매도 성공 직후 즉시 호출 (실패 시 미저장)
  - `run_loop()` 진입 시 `state = _load_state()`로 기존 초기화 코드 대체

### 변경 파일
- `src/config.py` — `get_state_path()` 함수 추가 (line 50-53)
- `src/scheduler.py` — `import json`, `date` 추가; `_load_state()`, `_save_state()` 신설; `_check_holdings()` 실현손익 수정; `run_loop()` 상태 로드 적용

### 자체 검증
- 코드 직접 읽어 구문·로직 수동 검증 완료
- ⚠️ **실행 검증은 Python 인터프리터 부재로 생략** → 세션 4 시작 시 `python -c "from src.scheduler import _load_state; print(_load_state())"` 로 import·초기값 확인 권장

---

## 2. 세션 4에서 주의해야 할 인계사항

1. **세션 1·2·3 API 사용 규칙 유지**
   - 환경변수: `from src import config; config.get_*()` 사용 (os.getenv 직접 호출 금지)

2. **`analyze()` 반환 구조 참고**
   - `src/analyzer.py`의 `analyze()` 반환값: `{"signal": "BUY/SELL/HOLD", "current_price": int, "reason": str, ...}`
   - 세션 4의 `⑧ analyze 보유종목 연동`에서 `avg_price` 실제값 전달 필요 → `main.py:66` 수정
   - `trader.get_holdings()` 호출로 보유종목 조회 가능 (세션 2에서 시그니처 미변경)

3. **`main.py` 수정 범위**
   - `⑧ analyze`: `main.py:66` 주변 — analyze 호출 시 avg_price 고정 0 → 실제값
   - `⑨ history --date`: `main.py:35` 주변 — `--date` 옵션 추가
   - 두 항목 모두 `main.py` 집중 변경

4. **`src/analyzer.py:10` RSI 변경**
   - `⑦ RSI Wilder SMMA`: SMA 방식 → Wilder's SMMA 방식으로 교체
   - `analyzer.py` 단독 변경, 다른 파일 영향 없음

---

## 3. 세션 4 작업 범위 (재확인)

| 항목 | 우선순위 | 대상 파일 |
|------|----------|-----------|
| ⑦ RSI Wilder SMMA | 🟡 Medium | `src/analyzer.py` |
| ⑧ analyze 보유종목 연동 | 🟡 Medium | `main.py` |
| ⑨ history --date 옵션 | 🟡 Medium | `main.py` |

---

## 4. 다음 세션 시작 프롬프트 (복사용)

```
/forge

C:\Users\lgw1156\Desktop\lgw\999_개인\kis-auto-cli\.doc\작업_플랜.md 와
C:\Users\lgw1156\Desktop\lgw\999_개인\kis-auto-cli\.doc\다음_세션_프롬프트.md 를 읽고
세션 [4]의 작업 범위만 진행한다.

[수행 단계]
1. 작업_플랜.md에서 이번 세션의 범위와 이전 세션의 인계사항을 확인한다.
2. 세션 범위에 대해 설계안을 제시하고 사용자 확인을 받는다.
3. 구현 → 자체 검증을 수행한다.
4. 결과 요약 + 다음 세션 인계 정보를 .doc/다음_세션_프롬프트.md 에 갱신한다.
   (마지막 세션이면 .doc/작업_완료_보고서.md 를 작성한다.)

제약사항:
- 이번 세션 범위 외 항목은 손대지 않는다.
- 각 단계에서 사용자 확인이 필요한 경우 진행을 멈추고 묻는다.
- 한국어로 응답한다.
```
